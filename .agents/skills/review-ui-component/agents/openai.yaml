interface:
  display_name: "Review UI Component"
  short_description: "Review component contracts, UX, and accessibility"
  default_prompt: "Use $review-ui-component to perform a read-only review of a component contract, UX, portability, and accessibility."
policy:
  allow_implicit_invocation: true
