interface:
  display_name: "Build UI Component"
  short_description: "Build a portable HTML, CSS, and JavaScript component"
  default_prompt: "Use $build-ui-component to implement a component with browser-ready HTML, CSS, JavaScript, or Tailwind under the repository contract."
policy:
  allow_implicit_invocation: true
